import { PortSearchPipe } from './port-search.pipe';

describe('PortSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PortSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
